name: .NET

on:
  workflow_dispatch:
  
  push:
    branches: master

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    
    - name: Navigate to Workspace
      run: cd $GITHUB_WORKSPACE
      
    - name: Build
      shell: pwsh
      run: |
        .\build.ps1
